What is included

  This repority has included fine-tuned ai models which is made for spesific roles(example for Education). Each one has a special talent on a topic.
They trained with real and high quality datasets. Also you can find voice control API request from ollama code. Its making requests with FastApi to ollama server. There is more project here, CNN training code to train CNN for spesific classification. 
